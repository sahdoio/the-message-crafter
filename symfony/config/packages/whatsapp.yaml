parameters:
    whatsapp.webhook_verify_token: '%env(WHATSAPP_WEBHOOK_VERIFY_TOKEN)%'
    whatsapp.access_token: '%env(WHATSAPP_ACCESS_TOKEN)%'
    whatsapp.base_url: '%env(WHATSAPP_BASE_URL)%'
    whatsapp.phone_number_id: '%env(WHATSAPP_PHONE_NUMBER_ID)%'
